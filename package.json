{
  "name": "screenshot-generator-ui",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "ramda": "^0.25.0",
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-scripts": "2.0.5",
    "serve": "^10.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "now-start": "serve -s build",
    "now-build": "react-scripts build && mv build dist",
    "deploy": "now --target=production"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "prettier-standard": "^8.0.1"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  },
  "now":{
    "name":"screenshot-generator-ui"
  }
}
